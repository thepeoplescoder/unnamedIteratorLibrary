import WrappedAsyncIterator from "@~main/iterators/classes/WrappedAsyncIterator";

import { describe, it } from "vitest";

describe(WrappedAsyncIterator.name, () => {
  it.todo("should be a function", () => { });
});