import { describe, it } from "vitest";
import {
  isActualObject,
  isIterator,
  isIterable,
  isGenerator,
  isIteratorOrGenerator,
  isAsyncIterator,
  isAsyncGenerator,
  isAsyncGeneratorOrAsyncIterator,
  isPromise,
} from "../../../../src/util/types/runtime/checks";

describe("isActualObject", () => { });
describe("isIterator", () => { });
describe("isIterable", () => { });
describe("isGenerator", () => { });
describe("isIteratorOrGenerator", () => { });
describe("isAsyncIterator", () => { });
describe("isAsyncGenerator", () => { });
describe("isAsyncGeneratorOrAsyncIterator", () => { });
describe(isPromise.name, () => {

});