import { describe, it } from "vitest";
import { isActualObjectWith, isIterator, isIterable } from "../../../../src/util/types/runtime/guards";

describe(isActualObjectWith.name, () => {
});

describe(isIterator.name, () => {
});

describe(isIterable.name, () => {
});