import { describe, it } from "vitest";
import WrappedAsyncIterator from "../../../src/iterators/classes/WrappedAsyncIterator";

describe(WrappedAsyncIterator.name, () => {
  it.todo("should be a function", () => { });
});